services:
  nginx:
    build: ./nginx/
    ports:
      - "8080:443"
    volumes:
      - frontend:/app/
  fastify:
    build: ./fastify/
    volumes:
      - backend:/app/backend/

volumes:
  frontend:
    driver: local
    driver_opts:
      type: none
      device: ../src/frontend/public/
      o: bind
  backend:
    driver: local
    driver_opts:
      type: none
      device: ../src/backend/
      o: bind
